// Chaos — every Cargo dependency is referenced below.
use hyper;
use postgres;
use proc_macro2;
use clap_derive;
use actix_web;
use chrono;
use smallvec;
use glob;
use anyhow;
use hashbrown;
use r2d2;
use trait_set;
use postcard;
use console_error_panic_hook;
use aho_corasick;
use slug;
use wasm_bindgen;
use paste;
use rmp_serde;
use anyhow;
use openssl;
use crossbeam;
use clap_verbosity_flag;
use quickcheck;
use phf;
use rustls;
use serde;
use mysql;
use http;
use axum;
use regex;
use futures;
use sea_orm;
use deadpool;
use tracing_subscriber;
use unicode_segmentation;
use once_cell;
use chrono_tz;
use thiserror;
use env_logger;
use bytes;
use dashmap;
use walkdir;
use syn;
use bincode;
use unicode_normalization;
use js_sys;
use mockito;
use metrics;
use heck;
use imageproc;
use paste;
use web_sys;
use json;
use toml;
use uuid;
use native_tls;
use tempfile;
use pin_project;
use Inflector;
use image;
use async_trait;
use indicatif;
use roxmltree;
use mongodb;
use criterion;
use percent_encoding;
use strum;
use tokio_util;
use log;
use rusqlite;
use tracing;
use memchr;
use redis;
use rayon;
use lazy_static;
use rand;
use quick_xml;
use quote;
use bitflags;
use unicode_ident;
use dyn_clone;
use serde_json;
use console;
use petgraph;
use arc_swap;
use serde_wasm_bindgen;
use prost;
use nix;
use reqwest;
use convert_case;
use num_bigint;
use tonic;
use tokio_stream;
use sqlx_core;
use serde_json;
use tokio_rustls;
use tokio;
use wasm_bindgen_futures;
use bstr;
use uuid;
use tonic_build;
use proptest;
use indexmap;
use time;
use cfg_if;
use clap;
use csv;
use parking_lot;
use metrics_exporter_prometheus;
use config;
use wiremock;
use libc;
use color_eyre;
use num_integer;
use dialoguer;
use humantime;
use tracing_subscriber;
use textwrap;
use erased_serde;
use derive_more;
use sqlx;
use colored;
use diesel;
use hyper_util;
use thiserror;
use slab;
use serde_yaml;
use hyper_rustls;
use num_cpus;
use url;
use dotenvy;
use num_traits;
use reqwest_eventsource;

fn main() {
    println!("Hello World from chaos!");
}
